#include<stdio.h>
int main()
{
    int fact=1,n ,i;
    printf("Enter the number :\n");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        fact=fact*i;
        printf("The factorial of %d is %d \n",i,fact);
    }
    
    return 0;
    
}
